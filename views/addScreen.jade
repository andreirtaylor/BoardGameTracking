extends layout

block content
    h1= title
    p Welcome to #{title}
    p. 
    p(id="counter") 
        {{ 1 + 1 }}
    input(type="text", ng-model="name")
    p {{ name }}
    div(ng-controller="AS")
        h1.
            {{name}}
        p {{money | currency}}
